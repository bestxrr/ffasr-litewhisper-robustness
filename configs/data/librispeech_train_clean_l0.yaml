repo_id: openslr/librispeech_asr
repo_type: dataset
files:
  - clean/train.100/0000.parquet
max_items: 1200
output:
  root: artifacts/manifests/librispeech_train_clean_l0
  audio_dir: audio
  manifest: manifest.jsonl
selection:
  min_seconds: 1.0
  max_seconds: 30.0
license_note: "LibriSpeech train-clean-100 shard 0000; used for Level-0 training only, disjoint from validation-clean proxy."
